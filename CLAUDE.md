# СЕТАП — дизайн-документ и правила проекта

> Этот файл читает Claude Code при каждой сессии. Здесь единственный источник правды по
> механикам, числам и ограничениям. Если требование задачи противоречит этому файлу —
> остановись и спроси, а не «догадайся».

---

## 1. Что это за игра

Кликер про сборку компьютерного места. Игрок — фрилансер с убитым офисным компом.
Клик = выполненная работа = деньги. Деньги = железо. Всё купленное железо **видно на
единственной сцене**: ЭЛТ-монитор превращается в три экрана, бежевый ящик — в стеклянный
корпус с водянкой, кухонный стол — в студию с подсветкой и окном в ночной город.

**Жанровый хук, отличающий игру от тысячи кликеров:** железо греется и потребляет
электричество. Клики нагревают систему; прогретая система работает быстрее, перегретая —
троттлит. Блок питания ограничивает, что вообще можно поставить. Это превращает «жми и
смотри на цифры» в удержание системы в рабочем режиме.

**Целевая платформа:** Яндекс.Игры (в первую очередь мобильный веб), затем Playgama.
**Целевая сессия:** 4–8 минут. **Целевое время до первого переезда (престижа):** 25–40 минут.

---

## 2. Технические рамки (не обсуждается)

- Чистый JavaScript, ES-модули, Canvas 2D. **Ноль внешних библиотек, ноль сборщиков.**
  Игра открывается двойным кликом по `index.html` и работает.
- Вся графика рисуется кодом. Растровых ассетов нет вообще (кроме звука).
- Разработка ведётся с телефона через Claude Code + GitHub, проверка через GitHub Pages.
  Значит: маленькие атомарные коммиты, каждый оставляет игру в рабочем состоянии.
- Бюджет: игра целиком (без звука) < 300 КБ, первый экран < 2 сек на среднем Android.

### Требования Яндекс.Игр — нарушение = отказ модерации

- Теги `<audio>` и `<video>` **запрещены**. Весь звук — только Web Audio API.
  Самопроверка: `document.querySelectorAll('audio, video').length === 0`.
- `ysdk.features.LoadingAPI.ready()` вызывается **до** того, как игра стала интерактивной.
- Язык берётся из SDK **до первого рендера UI**. Хардкод `'ru'` запрещён.
- Скролл страницы заблокирован: `position: fixed` на body + `preventDefault` на `touchmove`.
- Заголовок игры одинаков во всех полях. Без CAPS.
- Аудио — только CC0-сэмплы (Kenney Interface Sounds, OpenGameArt), формат `.wav`
  (`.ogg` нестабилен в Safari/iOS). Процедурно синтезированный звук модераторы Playgama
  помечают как AI-generated — не использовать.
- Интервал между интерстишлами ≥ 60 сек, реклама не показывается в первые 30 сек сессии.

---

## 3. Структура файлов

```
index.html
src/
  main.js            цикл, инициализация, склейка
  config.js          ВСЕ числа игры. Единственное место с константами.
  state.js           состояние, сериализация, миграции сейвов
  economy.js         формулы: доход, множители, ватты, цены
  systems/
    heat.js          нагрев, зоны, троттлинг
    dust.js          пыль и чистка
    combo.js         серия кликов
    orders.js        заказы клиентов        (фаза 3)
    prestige.js      переезд и репутация    (фаза 3)
    offline.js       оффлайн-доход          (фаза 3)
  render/
    scene.js         оркестратор кадра, камера, safe-zone
    layers/room.js desk.js tower.js monitor.js peripherals.js
    lighting.js      свет от монитора и подсветки, bloom
    fx.js            частицы, тряска, всплывающие числа
  ui/
    hud.js shop.js modals.js
  platform/
    sdk.js           обёртка над Yandex SDK + заглушка вне платформы
  i18n.js            ru / en
assets/sfx/*.wav     CC0
tools/sim.js         симулятор баланса (node)
```

Правило: **числа живут только в `config.js`.** Ни один магический множитель не пишется
внутри системы или рендера.

---

## 4. Экономика

### 4.1 Источники дохода

| Источник | Формула |
|---|---|
| Клик | `clickBase × mouseMult × crit × globalMult × heatMult × comboMult × boostMult` |
| Автоклик (макросы) | `клавиатура_cps × ramMult` кликов/сек, каждый по `0.6 ×` цены клика, без крита |
| Пассив (рендер на GPU) | `gpuIncome × globalMult × heatMult × boostMult` в секунду |

`globalMult = монитор × мебель × подсветка × декор × (1 + 0.02 × репутация)`

### 4.2 Тепло

- Зоны: `<35°` ×1.0 · `35–85°` ×1.5 (рабочая зона) · `85–100°` ×2.0 (риск) ·
  `>100°` троттлинг ×0.35, автоклики стоп, пассив вдвое слабее, до остывания ниже `55°`.
- Нагрев: `4°` за ручной клик, `1.2°` за автоклик, `2×CPU_lvl + 4×GPU_lvl + 1×RAM_lvl` в секунду фоном.
- Охлаждение: `(cooler[lvl] + ac[lvl]) × (1 − 0.5 × dust)` градусов в секунду.
- «Разгон» (кнопка, откат 33 сек): 8 секунд ×3 к доходу и ×2.5 к нагреву.

**Смысл системы:** игрок хочет держаться в 35–85°, а не остывать. Значит клики нужны
постоянно, а не залпами, и охлаждение — не «строчка в списке», а разрешение играть быстрее.

### 4.3 Ватты — второй ограничитель

Каждый компонент потребляет ватты: `W0 + Wk × lvl` (у видеокарты на уровне 0 — 0 Вт,
встроенная графика). Блок питания даёт лимит. **Покупка, превышающая лимит, блокируется**
с явным сообщением: «Не хватает 45 Вт. Нужен блок питания помощнее».

Первый затык по ваттам должен наступать на 2–4 минуте — это момент, когда игрок понимает,
что собирает компьютер, а не жмёт кнопку. Тепло — динамический ресурс момента, ватты —
статический ресурс планирования. Они не дублируют друг друга и намеренно тянут в разные
стороны: мощная видеокарта требует и блока питания, и охлаждения.

### 4.4 Пыль

`dust` растёт на `0.0025` в секунду (полное загрязнение ≈ 6.7 мин), кондиционер снижает
скорость на 20% за уровень. Пыль режет охлаждение до −50%. Чистка: тап по кнопке, затем
6 быстрых тапов по пыльным зонам корпуса на сцене (2–3 сек), или мгновенно за rewarded-ролик.

Пыль существует ради ритма: она даёт короткое активное действие, объясняет, почему нельзя
уйти и вернуться через час, и создаёт естественное место для рекламы, которую игрок хочет сам.

### 4.5 Комбо

Клик в течение окна `comboWindow` (базово 1.2 сек, наушники расширяют) наращивает серию.
`comboMult = 1 + min(combo, 50) × 0.03` (до ×2.5). Пауза — мгновенный сброс.
Это награда за темп, чтобы ручной клик не обесценился на фоне автокликов.

---

## 5. Апгрейды

14 категорий, по 8 уровней (0–7). Три вкладки магазина: **Железо · Периферия · Комната**.

Цена следующего уровня: `cost(n) = round(C0 × r^(n−1))`, эффект: `val(n) = V0 × m^n`
(либо аддитивно, где указано `+`).

### Железо

| Категория | Эффект | V0 / шаг | m | C0 | r | Ватты |
|---|---|---|---|---|---|---|
| Процессор | ₽ за клик | 1 | 2.30 | 30 | 4.0 | 25 + 15n |
| Видеокарта | ₽/сек пассивно | 0.5 | 2.55 | 120 | 4.3 | 0 при n=0, иначе 60 + 45n |
| Оперативка | ×к автокликам | 1 | 1.35 | 150 | 4.1 | 5 + 3n |
| Накопитель | оффлайн: % и часы | 20% / 2 ч | +10% / +1 ч | 200 | 3.9 | 4 + 2n |
| Блок питания | лимит ватт | 220 | +180 | 250 | 3.6 | — |
| Охлаждение | °/сек | 10 | +8 | 60 | 4.0 | 3 + 4n |

### Периферия

| Категория | Эффект | V0 / шаг | m | C0 | r | Ватты |
|---|---|---|---|---|---|---|
| Мышь | ×к клику; крит ×5 | 1; крит 0 | 1.45; крит +4% (макс 28%) | 40 | 4.0 | 1 |
| Клавиатура | автокликов/сек | 0 при n=0, иначе 0.5 | 1.50 | 90 | 4.2 | 2 + n |
| Монитор | ×ко всему доходу | 1 | 1.22 | 110 | 4.2 | 30 + 20n |
| Наушники | окно комбо, сек | 1.2 | +0.35 | 80 | 3.8 | 2 |

### Комната

| Категория | Эффект | V0 / шаг | m | C0 | r | Ватты |
|---|---|---|---|---|---|---|
| Мебель (стол + кресло) | ×ко всему | 1 | 1.18 | 200 | 4.1 | — |
| Подсветка | ×ко всему | 1 | 1.15 | 180 | 4.0 | 10 + 6n |
| Кондиционер | +°/сек, −пыль | 0 | +6 | 300 | 4.0 | 40 + 25n |
| Декор | ×ко всему; кот с 3 ур. | 1 | 1.16 | 260 | 4.3 | — |

**Числа выше — стартовая точка, а не догма.** Их подгоняет симулятор (см. §9) под целевые
тайминги: первая покупка < 15 сек, затык по ваттам 2–4 мин, все категории куплены хотя бы
раз к 8-й минуте, первый переезд 25–40 мин.

---

## 6. Долгая игра (фаза 3)

**Заказы.** Каждые 40–70 секунд приходит клиент: «сделай N кликов за 20 сек» / «продержи
температуру в рабочей зоне 15 сек» / «заработай X». Награда — 20 секунд текущего дохода ×1.5.
Заказы дают цель между покупками и превращают фоновое кликание в короткие спринты.

**Переезд (престиж).** Доступен при общем заработке ≥ 5 000 000. Железо сбрасывается,
игрок получает репутацию `R = floor(15 × (totalEarned / 5e6)^0.6)`, каждая единица даёт
+2% ко всему навсегда. Пять локаций: комната у родителей → съёмная квартира → студия →
офис → серверная. Каждая — новый фон, палитра и базовый множитель ×3. Переезд, а не
абстрактный «престиж», потому что визуальная награда здесь и есть смысл игры.

**Оффлайн-доход.** `passiveRate × накопитель% × min(часы_отсутствия, лимит_накопителя)`.
При возвращении — экран «пока тебя не было», кнопка «удвоить за ролик».

**Кот.** Появляется с 3 уровня декора. Ходит по комнате, иногда садится на клавиатуру
(автоклики стоп на 4 сек — тап сгоняет), тап по коту = «погладить»: +50% к клику на 30 сек,
откат 3 минуты.

---

## 7. Визуал

Задача сцены — чтобы через 10 минут игрок захотел показать её кому-то. Всё рисуется кодом.

**Композиция.** Логическая сцена 1000×700, из неё гарантированно видна центральная
safe-zone 720×700 (стол, монитор, системник). На широком экране показывается вся сцена,
на узком — обрезается по краям (cover). Магазин: снизу в портрете, справа в ландшафте.

**Слои снизу вверх:** стена и окно → мебель → стол → системник → монитор → периферия →
свет и bloom → частицы → передний план (спинка кресла, растение).

**Освещение — главный рычаг «дорогого» вида.**
- Свет от монитора: аддитивный градиент на стол и стену, цвет усредняется по тому, что
  сейчас на экране. Меняется в реальном времени.
- RGB-подсветка: цветные пятна на стене за столом, дышат в такт.
- Дешёвый bloom: светящиеся элементы рисуются во второй offscreen-холст, размываются через
  `ctx.filter = 'blur(14px)'` и накладываются композицией `lighter`. Один проход на кадр.
- Зерно: один раз генерируем шумовую текстуру 128×128 в offscreen, накладываем тайлом с
  `overlay`, альфа 0.05. Убирает «пластиковость» плоских заливок.
- Температура сцены: при нагреве вся палитра сдвигается в красный, при холодной системе —
  в синий. Полупрозрачный слой поверх кадра.

**Визуальные состояния.** У каждой категории 8 механических уровней, но **4 визуальных
состояния**: уровни 0–1 = вид A, 2–3 = B, 4–5 = C, 6–7 = D. Иначе объём рисования вырастет
вчетверо без выигрыша в читаемости.

**Момент покупки.** Не мгновенная подмена спрайта. Сверху падает коробка курьера, слегка
подпрыгивает, раскрывается, новый предмет появляется с overshoot-масштабом 1.25 → 1.0,
короткая тряска камеры, вспышка. Это главная награда игры — она должна быть заметной.

**Живой экран монитора.** Отдельный мини-рендер: баланс, полоса рендера, столбики нагрузки,
всплывающие сообщения клиентов, «Перегрев» красным при троттлинге. На ЭЛТ 0-го уровня —
зелёный моноширинный текст со строчной развёрткой.

**День и ночь.** Цикл 4 минуты. Ночью комната темнее, RGB и экран доминируют, +10% к доходу.
Днём в окне светло, охлаждение на 15% хуже. Даёт сцене жизнь и повод возвращаться.

**Частицы.** Пыль в луче лампы, монеты и числа при клике, искры при крите, волны жара и
красные всполохи при перегреве, конфетти при переезде.

---

## 8. Интерфейс

- HUD: баланс, за клик, в секунду, полоса температуры с размеченной рабочей зоной, ватты
  (занято / лимит), индикатор пыли, кнопка «Разгон».
- Магазин: три вкладки, карточка = иконка, название, текущий уровень (полоски прогресса),
  что даст следующий уровень, цена. Доступная покупка подсвечена, недоступная по ваттам —
  отдельным цветом с причиной.
- Цифры: тысячи → `K`, миллионы → `M`. Табличные цифры, никаких прыгающих ширин.
- Копирайт интерфейса: короткие фразы обычным регистром, без капса. «Не хватает 45 Вт»,
  а не «ОШИБКА: ПРЕВЫШЕН ЛИМИТ МОЩНОСТИ».
- Локализация ru/en, все строки в `i18n.js`, ни одной строки в коде рендера.

---

## 9. Баланс проверяется кодом, а не на глаз

`tools/sim.js` (node, без зависимостей) прогоняет модель игры на скорости×1000 при
заданном темпе кликов (3, 5, 0 кликов/сек) и печатает таблицу: время до каждого уровня
каждой категории, момент первого затыка по ваттам, момент первого троттлинга, время до
переезда, доход в минуту по 5-минутным отрезкам.

Симулятор запускается после любого изменения `config.js`. Целевые значения — в §5.

---

## 10. Границы (что НЕ делаем)

- Никакой мультиплеерности, лидербордов и покупок за реальные деньги.
- Никакого 3D, никаких внешних библиотек, никакого npm/сборки.
- Не больше 4 визуальных состояний на категорию.
- Не добавляем новых механик сверх описанных здесь. Идея по ходу работы → в раздел
  «Предложения» в конце PR, не в код.
- Не трогаем формулы из `config.js` ради «чтобы было веселее» — только по результатам
  симуляции, с приложенным выводом симулятора.

---

## 11. Порядок работ

- **Фаза 1 — ядро.** Архитектура, `config.js`, 14 категорий, тепло, ватты, пыль, комбо,
  магазин с вкладками, сохранение, адаптив, i18n, заглушка SDK. Графика — на уровне
  текущего прототипа, без нового освещения.
- **Фаза 2 — визуал.** Освещение, bloom, зерно, температура сцены, анимация доставки,
  день/ночь, частицы, звук CC0.
- **Фаза 3 — долгая игра.** Заказы, переезд и репутация, оффлайн-доход, кот, достижения.
- **Фаза 4 — платформа.** Yandex SDK: сейв в облако, rewarded и интерстишлы, LoadingAPI,
  язык, сборка архива, публикация. Затем Playgama Bridge.

Каждая фаза — отдельная задача и отдельный промпт. Не начинать следующую, пока предыдущая
не проверена на живом устройстве через GitHub Pages.
